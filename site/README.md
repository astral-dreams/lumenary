# Lumenary Website

Future public website for daily findings, observations, hypotheses, convergences, and synthesis essays.

The first version should consume Markdown from `publication/daily/`, `observations/`, and `syntheses/`.
